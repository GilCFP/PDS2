#ifndef MODULO_H
#define MODULO_H

#include <stdio.h>
#include <math.h>

/**
 * Calcula o seno de um ângulo fornecido em radianos
 *
 * @param angle O ângulo desejado
 * @return O seno
 **/
float calcular_seno(float angle);

/**
 * Calcula o cosseno de um ângulo fornecido em radianos
 *
 * @param angle O ângulo desejado
 * @return O cosseno
 **/
float calcular_cosseno(float angle);

/**
 * Calcula a tangente de um ângulo fornecido em radianos
 *
 * @param angle O ângulo desejado
 * @return A tangente
 **/
float calcular_tangente(float angle);

#endif /* MODULO_H */
